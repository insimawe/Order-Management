{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ServiceUrls": {
    "CouponAPI": "https://localhost:7001", //this is the URL of the Coupon API service - go check the launchSettings.json file in the Coupon API project
    "AuthAPI": "https://localhost:7002", //this is the URL of the Auth API service - go check the launchSettings.json file in the Auth API project
    "ProductAPI": "https://localhost:7000" //this is the URL of the Product API service - go check the launchSettings.json file in the Product API project
  }
}
